# Java (Tomcat) MySQL and Watson Vision Recognition Hackathon Example

This app uses the Java/Tomcat runtime on Bluemix to interact with a MySQL database, and
use the Watson Vision Recognition service.

